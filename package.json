{
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@google/clasp": "3.4.0",
    "eslint": "10.9.1",
    "prettier": "YOUR_INSTALLED_VERSION"
  }
}